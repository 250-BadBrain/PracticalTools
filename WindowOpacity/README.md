# 摸鱼神器
# WindowOpacity

一个 Windows 原生窗口不透明度调节工具。

## 功能

- 设置全局快捷键调高或调低当前前台窗口不透明度
- 设置全局快捷键恢复窗口不透明
- 支持自定义不透明度步进
- 轻量原生 GUI

## 默认快捷键

```text
Ctrl + Shift + ]：调高不透明度
Ctrl + Shift + [：调低不透明度
Ctrl + Shift + \：恢复不透明
```

## 构建

```bat
build.bat
```
